# Archive Manifest

This manifest tracks archive handling decisions.

## Archived snapshots
All historical `.zip` snapshots from the repository root and legacy `zip/` folder were consolidated into this directory.

## Retained reference archives in active source tree
None currently retained.

If a future reference archive must remain outside `artifacts/archive/`, add an entry with justification and a cleanup trigger.
